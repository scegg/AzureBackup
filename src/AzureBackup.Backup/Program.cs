// azbackup entry point — placeholder until implementation phase.
// Responsibilities (see docs/architecture.md):
//   scan -> diff(hash) -> pack -> compress -> encrypt -> split(volumes) -> upload(Archive)
//   then optional GC, retention enforcement, manifest/index upload (Hot).

Console.WriteLine("azbackup: not implemented yet");
return 0;
