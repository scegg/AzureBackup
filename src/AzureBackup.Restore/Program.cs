// azrestore entry point — placeholder until implementation phase.
// Responsibilities (see docs/restore.md):
//   connect(source) -> load+decrypt index/snapshots -> list backups ->
//   select snapshot -> full or selective(tree) -> rehydrate Archive packs ->
//   download -> decrypt -> decompress -> write to destination.

Console.WriteLine("azrestore: not implemented yet");
return 0;
